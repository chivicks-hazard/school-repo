import java.util.Scanner;

public class EXIII {
    public static void main(String[] args) {
        // EX. 3.1: Solve quadratic equation
        /* double a;
        double b;
        double c;
        double discriminant;
        double r1;
        double r2;
        Scanner input = new Scanner(System.in);

        System.out.println("Give the coefficients of the quadratic equation to be solved");
        System.out.print("Enter a: ");
        a = input.nextDouble();
        System.out.print("Enter b: ");
        b = input.nextDouble();
        System.out.print("Enter c: ");
        c = input.nextDouble();

        discriminant = Math.pow(b, 2) - (4 * a * c);

        if (discriminant > 0) {
            r1 = (-b - Math.sqrt(discriminant)) / (2 * a);
            r2 = (-b + Math.sqrt(discriminant)) / (2 * a);

            System.out.println("The roots of the equations are " + r1 + " and " + r2);
        } else if (discriminant == 0) {
            r1 = -b / (2 * 4);
            System.out.println("The equation has one root which is: " + r1);
        } else {
            System.out.println("The equation has no real roots");
        } */

        // EX. 3.4: Random month
        /* String[] months = {"January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"};
        int random = (int) Math.round(Math.random() * (12 - 1 + 1) + 1);

        System.out.println(months[random]); */

        

    }
}
